<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Document</title>
    <style>
      * {
        box-sizing: border-box;
      }

      html,
      body {
        margin: 0;
        /* padding: 20mm; */
      }

      .wrapper {
        font-family: Helvetica, sans-serif;
        margin: 0 auto;
        color: rgb(64, 64, 64);
      }

      .flex {
        margin: 0 auto;
        width: 90%;
        display: flex;
        justify-content: space-around;
        align-items: stretch;
      }

      .header {
        border-bottom: 1px solid black;
        padding-bottom: 100px;
        justify-content: stretch;
      }

      .header-col1 {
        width: 30%;
      }
      .header-col2 {
        width: 35%;
        /* margin-top: 10px; */
      }
      .header-col3 {
        width: 35%;
        /* margin-top: 20px; */
      }

      .author {
        /* margin-top: 70px; */
        font-weight: bold;
        font-size: 16px;
      }

      table {
        width: 90%;
        margin: 0 auto;
        border-collapse: collapse;
        padding-bottom: 100px;
      }

      thead {
        border-bottom: 1px solid black;
      }

      th,
      td {
        padding: 15px;
        text-align: left;
        vertical-align: top;
      }

      th {
        white-space: nowrap;
      }

      .right-align {
        text-align: right;
      }

      .invoice-footer {
        margin-top: 100px;
        padding-right: 30px;
        border-top: 1px solid black;
        justify-content: flex-end;
        page-break-inside: avoid;
      }
      .invoice-footer .right {
        padding: 0 20px;
      }

      .footer {
        border-top: 1px solid black;
        font-size: 10px;
        flex-wrap: wrap;
        justify-content: space-around;
        /* margin-top: 80%; */
        /* position: absolute;
        bottom: 20px;
        right: 5%; */
      }

      .footer-col {
        padding: 0 15px;
      }

      .bold {
        font-weight: bold;
      }

      .italic {
        font-style: italic;
      }

      .date {
        white-space: nowrap;
      }

      .address-header {
        font-weight: bold;
        line-height: 0.6;
        font-size: 15px;
      }

      .large-header {
        margin-top: 0;
        font-size: 36px;
        font-weight: 400;
      }
      @media print  {
        .invoice-footer {
          page-break-inside: avoid;
        }
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <header class="flex header">
        <div class="header-col1">
          <h2 class="author"><%= user.name %></h2>
          <p><%= user.position && user.position %></p>
        </div>
        <div class="header-col2">
          <h1 class="large-header">Faktura</h1>
          <p>
            <span class="bold">Fakturanummer: </span><%= invoice.invoiceNumber
            %>
          </p>
          <div>
            <h3 class="address-header">Mottagare:</h3>
            <p>
              <%= invoice.recipient.authority %><br />
              <%= invoice.recipient.street %><br />
              <%= invoice.recipient.zip %> <%= invoice.recipient.city %>
            </p>
          </div>
        </div>
        <div class="header-col3">
          <p class="date">
            <span class="bold">Fakturadatum:</span>
            <%= invoice.updatedAt.toISOString().substring(0, 10) %>
          </p>
          <p>
            <span class="bold">Uppdragsnummer:</span>
            <%= invoice.assignmentNumber %>
          </p>
          <p>
            <span class="italic">Vår referens:</span><br />
            <%= user.name %>
          </p>
          <% if (invoice.recipient.refPerson) {%>
          <p>
            <span class="italic">Er referens:</span><br />
            <%= invoice.recipient.refPerson %>
          </p>
          <% } %>
          <p>
            <span class="italic">Betalningsvillkor:</span><br />
            30 dagar netto
          </p>
        </div>
      </header>

      <main>
        <table>
          <thead>
            <tr>
              <th>Beskrivning</th>
              <th>Antal</th>
              <th>Enhet</th>
              <th class="right-align">à pris</th>
              <th class="right-align">Belopp (SEK)</th>
            </tr>
          </thead>
          <tbody>
            <% invoice.rows.forEach(row => { %>
            <tr>
              <td><%= row.description %></td>
              <td><%= row.quantity %></td>
              <td><%= row.unit %></td>
              <td class="right-align"><%= row.price %></td>
              <td class="right-align"><%= row.amount %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>

        <div class="flex invoice-footer">
          <div class="left">
            <p>Belopp före moms</p>
            <p>Total moms (<%= invoice.VAT * 100 %>%)</p>
            <p class="bold">Att betala</p>
          </div>
          <div class="right">
            <p class="right-align">
              <%= invoice.totalBeforeVAT.toFixed(2) %> kr
            </p>
            <p class="right-align">
              <%= (invoice.totalAfterVAT - invoice.totalBeforeVAT).toFixed(2) %>
              kr
            </p>
            <p class="bold right-align">
              <%= invoice.totalAfterVAT.toFixed(2) %> kr
            </p>
          </div>
        </div>
      </main>

      <!-- <footer class="flex footer">
        <div class="footer-col">
          <p>
            <span class="bold">Adress</span><br />
            <%= user.street %><br />
            <%= user.zip %> <%= user.city %>
          </p>
        </div>
        <div class="footer-col">
          <p>
            <span class="bold">Bankgiro:</span><br />
            <%= user.bankgiro %>
          </p>
        </div>
        <div class="footer-col">
          <p>
            <span class="bold">Telefon</span><br />
            <%= user.phone %>
          </p>
        </div>
        <div class="footer-col">
          <p>
            <span class="bold">Epost</span><br />
            <%= user.email %>
          </p>
        </div>
        <div class="footer-col">
          <p>
            <span class="bold">Organisationsnr</span><br />
            <%= user.registrationNumber %><br />
            Godkänd för F-skatt
          </p>
        </div> -->
      </footer>
    </div>
  </body>
</html>
